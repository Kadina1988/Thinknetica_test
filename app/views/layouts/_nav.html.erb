<% if user_signed_in? %>
  <p class="nav user">
    <%= t('.welcome', email: current_user.email) %>
  </p>
<% end %>

<div class='logout'><%= button_to t('.logout'), destroy_user_session_path, method: :delete %></div>

