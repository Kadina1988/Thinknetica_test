<h3><%= @test.title%></h3>
<table>
  <tr>
    <th>Question<th>
    <th></th>
  </tr>
   <% @test.questions.each do |question| %>
  <tr>
      <td><%= question.body %></td>
      <td><%= link_to 'open', question_path(question)%></td>
  </tr>
  <% end %>
</table>
<p><%= link_to 'new create', new_test_question_path(@test) %></p>
<p><%= link_to 'back', tests_path %></p>
